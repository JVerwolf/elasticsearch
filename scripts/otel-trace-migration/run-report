#!/usr/bin/env bash
# Convenience wrapper: run the trace attribute report for the qa environment.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/otm" qa report "$@"
